{
  "name": "server",
  "version": "0.16.1",
  "scripts": {
    "format": "prettier --write '{.,src/**}/*.{js,jsx,ts,tsx}'",
    "lint": "cd .",
    "prepare": "cd .. && yarn patch-package"
  },
  "devDependencies": {
    "prettier": "^1.15.2"
  }
}