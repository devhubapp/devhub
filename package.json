{
  "name": "devhub",
  "version": "0.12.3",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "scripts": {
    "compile": "yarn workspace shared compile",
    "dev": "yarn compile -w",
    "format": "yarn workspaces run format",
    "lint": "yarn workspaces run lint",
    "prepare": "patch-package"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^1.1.3",
    "lint-staged": "https://github.com/brunolemos/lint-staged",
    "patch-package": "^5.1.1",
    "postinstall-prepare": "^1.0.1",
    "prettier": "^1.14.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "tslint-react": "^3.6.0",
    "typescript": "^3.1.6"
  }
}
